### API Key & Security

*   **API Key**: Required to authenticate your requests. Your key is sent directly to the provider via `ellmer`.
*   **Save in Cookies**: If enabled, the key is stored locally in your browser's cookies to persist across sessions. It is never stored on the application's server.
*   **Test Connection**: Use this to verify that your key and model name are valid before starting the analysis.

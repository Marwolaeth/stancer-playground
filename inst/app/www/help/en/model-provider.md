### Select Provider

Choose the API service you wish to use. The application uses the `ellmer` library to communicate with various LLM backends. You can use direct providers (like OpenAI or Anthropic) or aggregators like OpenRouter.
